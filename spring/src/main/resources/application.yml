spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://host:3306/database-name
    username: seu_user
    password: sua_senha
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true